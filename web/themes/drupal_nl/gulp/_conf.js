'use strict'

export const dirs = {
  src: 'src',
  dist: 'dist'
}

export const paths = {
  local: `http://dnl.local`,
  styles: {
    src: `${dirs.src}/sass/**/*.scss`,
    dist: `${dirs.dist}/css`
  }
}
